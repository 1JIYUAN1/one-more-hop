{
  "name": "one-more-hop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A one-button risk-reward jumping game with bank-or-continue decisions.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
